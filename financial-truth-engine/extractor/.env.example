# Copy to .env and fill in real values. Never commit .env.
DATABASE_URL=
PROVIDER_API_KEY=
# Task 006K: name reserved for a future live OpenAI client (Task 006L).
# Not read or required by any Task 006K code path — no live client is
# constructed in Task 006K.
OPENAI_API_KEY=
